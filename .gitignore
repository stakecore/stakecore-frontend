# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules

# pnpm: project-local content-addressable store (created when the global
# store sits on a different filesystem). The lockfile and patches/ stay tracked.
.pnpm-store/

dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*:Zone.Identifier

*.env
.claude/settings.local.json

# Harness-created git worktrees (EnterWorktree). Each one is a full checkout
# of the repo, so leaving this untracked means a stray `git add -A` on the
# branch you're back on commits a second copy of the tree into it. The
# branches the worktrees sit on are ordinary refs and stay tracked as normal.
.claude/worktrees/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Superpowers scratch: per-plan ledgers, task briefs, agent reports, review
# packages. Working state for a run, not a record of it — the record is the
# commits. The sdd/ subtree self-ignores, but that only covers what today's
# scripts happen to write; anchor the whole directory instead.
/.superpowers/
